# 变更计划 · YYYY-MM-DD

> 目标：一句话说清要达成什么（修什么 bug / 加什么能力）

## 计划修改

| 文件 | 位置 | 改动点 | 理由 |
|------|------|--------|------|
| `src/xxx.ts` | L42 | 增加空值判断 | 修复 #123 描述的崩溃 |
| `src/yyy.ts` | L18 | 调整参数顺序 | 与上面的改动配套 |

## 不会改动

- 清单外的任何文件（包括风格不一致、看着别扭的部分）
- 任何依赖版本（`package.json` / `requirements.txt` / `go.mod` 等）
- 未被要求的注释、日志、配置项、抽象层

## 风险与影响

- 无
- 或：本次改动会影响 `zzz()` 的返回值，调用方有 3 处（已列出，待确认是否一并调整）

## 需要确认的点

- 无
- 或：改到 `src/yyy.ts` 超出原计划，是否允许？

---

确认后执行。执行完毕输出改动摘要 + 自检结果。
